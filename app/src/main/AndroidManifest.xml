<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.jdamiancabello.inventory">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.sector.SectorActivity"></activity>
        <activity android:name=".ui.dependency.DependencyActivity" />
        <activity
            android:name=".ui.SignUpActivity"
            android:label="@string/activitySignUp"
            android:launchMode="singleTop" />
        <activity
            android:name=".ui.SplashActivity"
            android:theme="@style/SplashTheme">
        </activity>
        <activity
            android:name=".ui.DashBoardActivity"
            android:label="@string/activityDashboardLabel"
            android:launchMode="singleTask" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.LoginActivity"
            android:label="@string/activityLoginName"
            android:theme="@style/AppTheme" />
    </application>

</manifest>